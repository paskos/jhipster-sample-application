apply plugin: "war"

bootWar {
    mainClassName = "org.paskos.myapp.JhipsterSampleApplicationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
